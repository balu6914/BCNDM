syntax = "proto3";

package monetasa;

service AuthService {
    rpc Identify(Token) returns (UserID) {}
    rpc Email(Token) returns (UserEmail) {}
}

message Token {
    string value = 1;
}

message UserID {
    string value = 1;
}

message UserEmail{
    string value = 1;
}
