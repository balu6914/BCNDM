syntax = "proto3";

package monetasa;

import "common.proto";

service StreamsService {
    rpc One(ID) returns (Stream) {}
}

message Stream {
    string id = 1;
    string owner = 2;
    uint64 price = 3;
}
