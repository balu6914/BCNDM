apiVersion: v1
kind: Service
metadata:
  name: "{{ include "datapace.fullname" . }}-{{ .Values.transactions.suffix }}"
  labels:
    {{- include "datapace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.transactions.service.type }}
  ports:
    - port: {{ .Values.transactions.service.port.http }}
      targetPort: {{ .Values.transactions.service.port.http }}
      protocol: TCP
      name: http
    - port: {{ .Values.transactions.service.port.grpc }}
      targetPort: {{ .Values.transactions.service.port.grpc }}
      protocol: TCP
      name: grpc
  selector:
    app: {{ include "datapace.fullname" . }}
    component: {{ .Values.transactions.suffix }}
