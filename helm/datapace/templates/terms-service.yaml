apiVersion: v1
kind: Service
metadata:
  name: "{{ include "datapace.fullname" . }}-{{ .Values.terms.suffix }}"
  labels:
    {{- include "datapace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.terms.service.type }}
  ports:
    - port: {{ .Values.terms.service.port.http }}
      targetPort: {{ .Values.terms.service.port.http }}
      protocol: TCP
      name: http
    - port: {{ .Values.terms.service.port.grpc }}
      targetPort: {{ .Values.terms.service.port.grpc }}
      protocol: TCP
      name: grpc
  selector:
    app: {{ include "datapace.fullname" . }}
    component: {{ .Values.terms.suffix }}
