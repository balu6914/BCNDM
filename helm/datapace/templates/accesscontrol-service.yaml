apiVersion: v1
kind: Service
metadata:
  name: "{{ include "datapace.fullname" . }}-{{ .Values.accesscontrol.suffix }}"
  labels:
    {{- include "datapace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.accesscontrol.service.type }}
  ports:
    - port: {{ .Values.accesscontrol.service.port.http }}
      targetPort: {{ .Values.accesscontrol.service.port.http }}
      protocol: TCP
      name: http
    - port: {{ .Values.accesscontrol.service.port.grpc }}
      targetPort: {{ .Values.accesscontrol.service.port.grpc }}
      protocol: TCP
      name: grpc
  selector:
    app: {{ include "datapace.fullname" . }}
    component: {{ .Values.accesscontrol.suffix }}
