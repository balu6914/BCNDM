apiVersion: v1
kind: Service
metadata:
  name: "{{ include "datapace.fullname" . }}-{{ .Values.subscriptions.suffix }}"
  labels:
    {{- include "datapace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.subscriptions.service.type }}
  ports:
    - port: {{ .Values.subscriptions.service.port.http }}
      targetPort: {{ .Values.subscriptions.service.port.http }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "datapace.fullname" . }}
    component: {{ .Values.subscriptions.suffix }}
